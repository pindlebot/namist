#!/usr/bin/env node
const namer = require('../lib/index')
const yargs = require('yargs')

let _ = yargs
  .command(
    '$0 [seeds]',
    'Generate names',
    () => {},
    argv => {
      let list = namer(argv.seeds || [], {})
      console.log(JSON.stringify(list, null, 2))
    }
  ).argv
